# Copy to `.env` (which is git-ignored) and fill in. Never commit real keys.

# --- Tier 1: the brain (required) ---
ANTHROPIC_API_KEY=

# --- Tier 3: voice (only needed for push-to-talk) ---
DEEPGRAM_API_KEY=
ELEVENLABS_API_KEY=
